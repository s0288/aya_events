#!/usr/bin/python3.6
# coding: utf8

from setuptools import setup, find_packages

setup(
   name='aya_events',
   version='1.0',
   description='A package to handle group events with aya',
   author='s0288',
   packages=find_packages()
)